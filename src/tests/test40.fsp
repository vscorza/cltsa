set Global_Alphabet = {a,b,c,d}
set Controllable_Alphabet = {a,b}

A = (<a,b> -> d -> A | d -> A).
B = ( c -> B | <b> ->B).

|f| INTERLEAVED = (A || B).
|f| CONCURRENT = (A ||_c B).
|f| SEQUENTIAL = (A ||_s B).

